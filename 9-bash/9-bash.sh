#!/bin/bash

# First we have to specify the path where we want to conduct the search
# Then we can select an option; to specify what the name of the file should look like in this case
# Search will be recursive so subdirectories will also be processed
find /home/jonathan -name *.txt

# from home path path
find /home/jonathan -name Documents